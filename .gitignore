# Secreto: la URL del webhook de Bitrix24 vive solo en el servidor, nunca en el repo
bitrix-config.php

# Local / herramientas
.claude/

# Artefacto de deploy (se genera para subir al hosting; no versionar)
*.zip
